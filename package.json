{
  "name": "ssb-uri",
  "version": "1.0.0",
  "description": "convert between SSB sigil links and SSB URIs",
  "main": "index.js",
  "scripts": {
    "lint": "standard --fix",
    "test": "node test/*.js"
  },
  "author": "Fraction",
  "license": "ISC",
  "dependencies": {
    "ohm-js": "^0.14.0"
  },
  "devDependencies": {
    "standard": "^12.0.1",
    "tape": "^4.9.1"
  }
}
